# 프론트엔드 스타일 전면 개편 계획

2026-08-14에 비교표 기반 Q&A를 거쳐 확정한, 이 앱의 모든 화면을 대상으로 한
전면 비주얼 개편 계획서입니다. 이 문서는 그 작업의 설계 기록이며, 실제
구현은 별도로 디스패치되는 세션에서 진행합니다(아래 "붙여넣기용 프롬프트"
참고) — 각 세션은 작업 전에 반드시 이 문서를 먼저 읽어야 합니다.

**상태 (2026-08-14): 7개 페이지 전환 항목 전부 완료** — 항목별 랜딩 기록은
아래 "페이지별 작업 목록"을, 계획 차원의 기록은 [ROADMAP.ko.md](../../docs/ROADMAP.ko.md)
> 7을 참고하세요. 초기 점검에서 함께 드러난 관련 항목 2건은 의도적으로 범위
밖에 남겨져 여전히 열려 있습니다 — 아래 "범위 밖이지만 관련된 사항"과 "확인
필요 사항" 참고.

> **이후 변경 (2026-08-24)**: 항목 4의 파일 보드는 더 이상 그 행의 설명대로
> 생기지 않았습니다 — 텍스트 목록과 Previous/Next 페이저가 3열 프리뷰 그리드 +
> 무한 스크롤로 대체됐습니다. 이 문서는 2026-08-14 오버홀이 무엇을 결정하고
> 무엇을 내놓았는지에 대한 기록으로 그대로 두되, 읽는 사람이 이를 현재 구현으로
> 오인하지 않도록 아래 행에 안내를 붙였습니다. 전체 내용:
> [CHANGELOG.ko.md](../../docs/CHANGELOG.ko.md) `[Unreleased] > 변경`.

## 이번 세션에서 확정한 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 스타일링 방식 | **CSS Modules** (`*.module.css`, 컴포넌트별 동일 위치 배치) | 신규 의존성 없음 — Vite가 `*.module.css`를 기본 내장 지원하므로, Tailwind/styled-components와 달리 frontend/CLAUDE.md의 "CSS 프레임워크 도입 전 제안 필요" 조건에 걸리지 않음. 순수 CSS 문법을 스코프 단위로 유지하면서, 현재의 인라인 `style={{}}` 컨벤션은 대체함. |
| 비주얼 방향 | 브랜드 지향 + 명시적 다크/라이트 토글 | 사용자 선택. 현재 `index.css`의 OS 설정(`prefers-color-scheme`) 의존 방식을 넘어서는 요구. |
| 팔레트 출처 | 이 세션이 제안, **2026-08-14 확정**(아래) | 이 저장소에 참조할 기존 브랜드 자산(로고, 스타일 가이드)이 없음. |
| 적용 범위 | 전체 5개 라우트 페이지 + 공통 `NavBar` | 사용자 선택 — 이번 패스에서 제외되는 페이지 없음. |
| 문서 위치 | `frontend/docs/STYLE-PLAN.md` (+ `.ko.md`) | 사용자 선택. |

## 조사 요약 (현재 상태)

현재 모든 화면은 인라인 `style={{...}}` 객체로 스타일링되어 있습니다 —
`frontend/src` 어디에도 CSS Modules/Tailwind/styled-components가 쓰이고
있지 않습니다. `index.css`에는 이미 작은 규모의 CSS 커스텀 프로퍼티
토큰 세트(`--text`, `--text-h`, `--bg`, `--border`, `--accent`, `--shadow`
등)와 `@media (prefers-color-scheme: dark)` 오버라이드 블록이 정의돼
있습니다 — 새 토큰 체계는 이것을 대체하는 것이 아니라 이 씨앗을
확장하는 방향이어야 합니다.

| 페이지 (라우트) | 파일 | 라인 수 | 비고 |
|---|---|---|---|
| 로그인/회원가입 (`/login`) | `features/auth/LoginPage.tsx` | 96 | 중앙 정렬된 단순 카드 폼. |
| 게시글 게시판 (`/`, 홈) | `features/posts/PostBoard.tsx`, `PostForm.tsx`, `FilePicker.tsx` | 210 + 84 + 109 | 새 글 작성 폼, 검색/정렬/페이지네이션 목록. |
| 게시글 상세 (`/posts/:id`) | `features/posts/PostDetailPage.tsx`, `CommentThread.tsx`, `CommentForm.tsx` | 294 + 209 + 65 | 인라인 수정/삭제, 첨부 파일 재생, 댓글 스레드. |
| 파일 게시판 (`/files`) | `features/files/DashboardPage.tsx`, `FileBoard.tsx`, `UploadForm.tsx` | 24 + 200 + 147 | 업로드 폼, 검색/정렬/페이지네이션 목록. |
| 파일 상세 (`/view/:id`) | `features/files/FileDetailPage.tsx`, `VisibilityBadge.tsx` | 276 + 31 | 영상/오디오/이미지 재생, 가시성 및 공유 토큰 관리. |
| 공통 네비게이션 | `shared/NavBar.tsx` | 40 | 인증된 모든 화면에서 렌더링. |

합계: 13개 파일, 약 1,785줄이 어떤 형태로든 인라인 스타일을 사용 중입니다.

## 브랜드 팔레트 (2026-08-14 확정)

`index.css`에는 이미 포인트 퍼플(`#aa3bff` 라이트 / `#c084fc` 다크)이
씨앗으로 심어져 있습니다. **확정: 이 색을 브랜드 컬러로 유지**하고(이미
실제 적용 중이라 위험이 가장 낮고, 영상/파일/게시글 관리라는 "창작 도구"
느낌에도 퍼플이 어울림) 전혀 다른 색조로 바꾸는 대신 더 완전한 토큰
체계로 확장했습니다.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--brand` | `#8a2be2` | `#c084fc` | 주요 버튼, 활성 네비게이션 링크, 링크 |
| `--brand-hover` | `#7422c9` | `#d4a6fd` | 위 요소의 hover/active 상태 |
| `--brand-contrast` | `#ffffff` | `#1b0e2e` | `--brand` 위에 올라가는 텍스트/아이콘 색 |
| `--surface` | `#ffffff` | `#16171d` | 페이지 배경 |
| `--surface-raised` | `#f8f7fb` | `#1f2028` | 카드, 폼 패널, 테이블 행 |
| `--border` | `#e5e4e7` | `#2e303a` | 구분선, 입력창/카드 테두리 |
| `--text` | `#3f3a46` | `#c8c6d0` | 본문 텍스트 |
| `--text-muted` | `#6b6375` | `#9ca3af` | 보조/메타 텍스트(이메일, 타임스탬프) |
| `--text-heading` | `#08060d` | `#f3f4f6` | h1/h2 |
| `--success` | `#1a7f37` | `#3fb950` | 예: "replayed"/업로드 완료 상태 |
| `--danger` | `#c92a2a` | `#f87171` | 삭제 버튼, 에러 텍스트 |
| `--danger-bg` | `#fdecec` | `#3b1b1b` | 에러 배너 |

이 표는 `index.css`의 현재 토큰 블록을 **대체하며 확장**합니다 — 기존
블록과 나란히 두는 것이 아닙니다.

## 다크/라이트 모드 토글 (설계 스케치)

현재 상태: `index.css`는 `prefers-color-scheme`만 따르며, 앱 내 토글도
영속화도 없습니다. 명시적 전환 스위치를 추가하려면:

- `<html>`에 `data-theme="light" | "dark"` 속성을 두고, 기본값은 미설정
  상태로 두어(오늘과 동일하게 `prefers-color-scheme`로 폴백).
- `:root[data-theme="dark"] { ... }` 오버라이드를 기존
  `@media (prefers-color-scheme: dark)` 블록과 동일한 토큰 값으로 미러링 —
  OS 설정 변경 없이도 다시 트리거할 수 있게 함.
- 작은 `ThemeProvider`(`src/theme/`, 신규 폴더)를 두어 현재 선택을 React
  상태로 들고, `localStorage`(`ui-theme`)에 미러링해 새로고침 후에도
  사용자가 명시적으로 고른 값을 유지 — 저장된 값이 없으면
  `prefers-color-scheme`로 폴백.
- `NavBar`에 토글 컨트롤 추가.

이것은 작은 신규 추상화(컨텍스트 프로바이더)이며 Scope Discipline에 따라
여기 명시합니다 — 다만 "다크/라이트 모드 구현"(OS 레벨이 아닌 명시적
토글)을 사용자가 이미 구체적으로 요청했으므로 별도 승인 없이 진행합니다.

## 페이지별 작업 목록

| # | 페이지/컴포넌트 | 작업 |
|---|---|---|
| 1 | ~~토큰 기반 작업~~ | **2026-08-14 완료** (CHANGELOG.md 참고). `index.css`의 토큰 블록을 위 팔레트로 교체; `ThemeProvider` + `data-theme` CSS 추가; NavBar를 제외한 페이지 레이아웃은 비주얼 변경 없음(브라우저에서 직접 검증). |
| 2 | ~~`NavBar`~~ | **2026-08-14 완료**, 1번과 같은 작업에서 함께 처리(CHANGELOG.md 참고). CSS Module로 전환; 테마 토글 컨트롤 추가. |
| 3 | ~~`LoginPage`~~ | **2026-08-14 완료** (CHANGELOG.md 참고). CSS Module로 전환; 카드/입력창/모드 전환 링크 재디자인. |
| 4 | ~~`FileBoard` + `DashboardPage` + `UploadForm`~~ | **2026-08-14 완료**, 3번과 같은 작업에서 함께 처리(CHANGELOG.md 참고). CSS Modules로 전환; 업로드 폼, 필터 바, 파일 목록 행, 페이지네이션 재디자인. **2026-08-24 일부 대체**: 여기서 만든 "파일 목록 행"과 "페이지네이션"은 사라졌습니다 — 3열 16:9 프리뷰 그리드(`FilePreviewTile`) + 무한 스크롤이 그 자리를 대신했고, 프레임 크기를 확보하려고 페이지 폭도 720px → 1126px로 넓혔습니다. 근거는 요즘 기기 성능이면 파일을 받아오는 것 자체는 무리가 없고 스크롤이 텍스트 목록보다 빠르게 읽히지만, 프리뷰가 쌓여 성능 저하가 되지는 않도록 180개 자동 로드 상한과 영상 클릭 게이트를 둔다는 것입니다. 이 행이 완료한 CSS Modules 전환 자체는 그대로 유효합니다. |
| 5 | ~~`FileDetailPage` + `VisibilityBadge`~~ | **2026-08-14 완료** (CHANGELOG.md 참고). CSS Modules로 전환; 헤더/플레이어/공유 링크/Manage 패널 재디자인; 제목 겹침 버그를 근본 원인 수준에서 수정(아래 참고). |
| 6 | ~~`PostBoard` + `PostForm` + `FilePicker`~~ | **2026-08-14 완료** (CHANGELOG.md 참고). CSS Modules로 전환; 새 글 작성 폼, 파일 첨부 피커, 게시글 목록 행, 페이지네이션 재디자인 — 4번 항목 `FileBoard`와 동일한 필터/목록/페이지네이션 구조를 그대로 따르되, `PostBoard`는 (`DashboardPage`와 달리) NavBar/제목/폼/목록을 한 파일에서 모두 호스팅하므로 페이지 래퍼 클래스를 추가했습니다. |
| 7 | ~~`PostDetailPage` + `CommentThread` + `CommentForm`~~ | **2026-08-14 완료** (CHANGELOG.md 참고). CSS Modules로 전환; 게시글 헤더, 수정/삭제 컨트롤, 댓글 스레드(목록, 인라인 수정/삭제), 댓글 작성 폼 재디자인 — 5개 페이지 중 마지막. 한글로 하드코딩된 UI 문자열은 그대로 유지(별도 i18n 결정 사안, 아래 참고). 5번 항목이 "이후 지울 것"으로 남겨둔 `PostDetailPage.tsx`의 불필요해진 인라인 `lineHeight: 1.25` `<h1>` 오버라이드도 이번에 함께 제거했습니다. |

권장 디스패치 순서는 번호 순서와 동일합니다 — 이후 모든 페이지가 토큰
체계 존재를 전제하므로 토큰 기반 작업을 가장 먼저 처리합니다.

## 이번 작업으로 해결된 사항

- **파일 상세 제목 겹침** — 5번 항목 작업으로 **2026-08-14 해결**. 근본
  원인을 확인했습니다: 전역 `h1` 규칙(`index.css`)이 `font-size: 56px`만
  지정하고 자체 `line-height`는 없어서, `:root`의 `line-height: 145%`가
  *루트* 폰트 크기(18px ≈ 26px) 기준으로 계산된 값을 그대로 물려받았습니다 —
  56px 글자에 비해 턱없이 작은 값이라 제목이 줄바꿈되면 줄끼리 겹쳐
  보였습니다. `h2`가 이미 쓰던 비율과 맞춰 전역 `h1` 규칙 자체에
  `line-height: 118%`를 명시해 해결했습니다 — 페이지별 오버라이드가 아니라
  근본 원인을 고친 것으로, 아래 진단이 예상했던 방향과 일치합니다.
  **수정 중 함께 발견한 사실**: `PostDetailPage.tsx`는 게시글 상세 페이지가
  나올 때 이미 같은 버그를 겪었고, 그 `<h1>` 하나에만 인라인
  `lineHeight: 1.25`를 임시로 걸어 우회했습니다(CHANGELOG.md의 게시글 상세
  항목 참고) — 전역 규칙이 해당 문제를 커버하므로 그 인라인 오버라이드는
  불필요해졌지만, `PostDetailPage`는 5번이 아니라 7번 항목의 파일이라 당시
  작업에서는 일부러 손대지 않았습니다. **2026-08-14, 7번 항목이 해당 페이지를
  CSS Modules로 전환하며 함께 제거했습니다.**

## 범위 밖이지만 관련된 사항 (2026-08-14 UI/UX 점검 중 발견)

별도로 요청되지 않는 한 이번 스타일 작업에 포함되지 않습니다 — 재디자인을
위해 해당 파일을 만질 사람이 보게 될 것이므로 여기 기록만 해둡니다:

- **영상 재생 S3 CORS 오류**: `GET /file/:id/content`가 presigned S3 URL로
  리다이렉트하는데(백엔드 ADR 0036) 브라우저가 이를 fetch하지 못함 —
  버킷에 프론트엔드 origin을 허용하는 CORS 정책이 없음. 이것은 프론트엔드나
  백엔드 소스 코드가 아니라 AWS 버킷 설정 문제 — 별도 작업, 별도 담당.
  **2026-08-15 원인 규명**: 정확히는 `FileDetailPage.tsx`의 private 티어
  blob-fetch 경로(`src/api/client.ts`의 `requestBlob()`)가 리다이렉트의
  교차 출처 본문 읽기에서 걸리는 것이며, `pnpm test:e2e`의
  `detail.spec.ts:73` 실패와 별개의 결함이 아니다. 전체 추적 내용과 미확정
  후보 해결책 두 가지: ADR 0036 > "추가 기록 (2026-08-15)" 참고.
  **두 후보 해결책 모두 2026-08-16에 처리됨**: 버킷 CORS를 설정해 Playwright로
  실제 재검증해 private 티어 재생이 정상 동작함을 확인했고, 같은 날
  `detail.spec.ts:73`의 오래된 리다이렉트-구간 단언도 고쳤다 — 이 발견 사항에서
  남은 것은 없다. ADR 0036 > "추가 기록 (2026-08-16)" 참고.
- **한글/영어 UI 텍스트 혼용**: `features/posts/*`(PostForm, PostDetailPage,
  CommentThread, CommentForm)는 UI 문자열과 에러 메시지가 한글로
  하드코딩돼 있고, `features/auth/*`와 `features/files/*`는 영어만
  사용합니다. 스타일 작업 도중 암묵적으로 결정할 i18n 프레임워크 사안이
  아님 — 별도 작업.

## 확인 필요 사항

1. ~~제안된 팔레트 확정~~ — **2026-08-14 확정**, 기존 퍼플 브랜드 시드를
   유지. 1번 항목 디스패치 가능.
2. 위 "범위 밖이지만 관련된 사항"에 남은 2건을 이후 별도 작업으로 진행할지,
   의도적으로 그대로 둘지 확인.
3. **타이포그래피는 사실 결정된 적이 없다** (2026-08-25 기록) — 위 계획은
   팔레트를 확정하고 방향을 "브랜드 전면"이라 불렀지만, `index.css`는
   `--heading`과 `--sans`를 **바이트 단위로 동일하게**
   (`system-ui, 'Segoe UI', Roboto, sans-serif`) 정의한다. 헤딩 토큰이 존재하되
   아무것도 표현하지 않으므로, 브랜드 전면이라는 방향 전체를 지금은 강조색
   하나가 떠받치고 있다. 후속 작업은 **탐구를 먼저 하고 그다음 구현**하며, 어떤
   선택지도 미리 확정하지 않는다: 호스팅 웹폰트, 셀프호스팅 서체, 시스템 폰트
   유지가 모두 살아 있는 후보다. 그 작업이 결정하는 게 아니라 물려받는 제약이
   둘 — `frontend/CLAUDE.md`는 의존성 추가 전 제안을 요구하며(호스팅 폰트도
   해당), 위 팔레트는 확정이므로 이건 타이포그래피 문제이지 브랜드 색을 다시
   여는 일이 아니다. 추적: [ROADMAP.ko.md](../../docs/ROADMAP.ko.md) > 7.
4. **모션 전무, 깊이는 한 곳뿐** (2026-08-25 기록, 3번과 함께) —
   `transition`·`animation`·`@keyframes`가 `src/`의 모든 스타일시트에 **0회**
   등장해 hover·focus가 전부 즉시 점프하고, `--shadow`는 정확히 한 곳
   (`LoginPage.module.css`의 카드)에만 적용돼 나머지 표면은 모두 평면이다. 둘 다
   토큰으로 한 번에 넣을 수 있을 만큼 저렴하고, 서체 교체가 표현 계층의 나머지를
   함께 정리하기에 자연스러운 시점이므로 3번과 묶어 둔다.
5. **로그인 마크가 스타터 템플릿의 로고다** (2026-08-25 기록, 3번과 묶음) —
   Sharenpo 이름 변경(`0a14039`)이 `<img src="/favicon.svg">`와 워드마크로 로그인
   락업을 구성했다. 이름 변경 도중 마크를 새로 만드는 대신 있는 것을 재사용한
   판단이었다. 그런데 `frontend/public/favicon.svg`는 이 프로젝트에서 나온 게
   아니라 2026-07-24 Vite 스캐폴드 커밋(`6950034`)에서 왔다 — 디자인 툴에서
   export한 보라색 번개 도형이고, 같은 커밋의 `public/icons.svg`는 심볼이
   `bluesky`·`discord`·`github`·`x`·`documentation`·`social`이다. 실측된 결과 둘 —
   마크는 `#863bff`인데 `--brand`는 `#8a2be2`/`#c084fc`라 락업 안에 **세 번째**
   보라가 들어앉았고, `icons.svg`는 `src/`와 `index.html` 어디에도 참조가
   **0건**이다. 열린 결정: 진짜 마크를 만들 것인가, 아이콘을 걷어내고 워드마크만
   세울 것인가. `icons.svg` 삭제는 어느 쪽이든 결정이 필요 없다. 3번과 묶는 이유는
   워드마크의 서체와 마크가 하나의 디자인 결정이기 때문이다 — 나누면 락업을 두 번
   설계하게 된다. 추적: [ROADMAP.ko.md](../../docs/ROADMAP.ko.md) > 7.

## 붙여넣기용 프롬프트

이 세션의 메모리(`session-prompts.md`, "Ready-to-paste: frontend style
overhaul" 항목)에 기록된, `frontend/CLAUDE.md`가 적용되는 디스패치
프롬프트를 참고하세요 — 각 프롬프트는 작업 전에 이 문서를 먼저 다시
읽도록 되어 있어, 새 세션이 결정 과정을 처음부터 다시 유추할 필요가
없습니다.
