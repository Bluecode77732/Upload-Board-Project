apiVersion: v1
kind: Service
metadata:
  name: nginx-cluster-ip-service
spec: 
  selector:
    app: nginx
  type: ClusterIP
  ports:
    # Cluster Port
    - port: 80
      # Container Port
      targetPort: 80
