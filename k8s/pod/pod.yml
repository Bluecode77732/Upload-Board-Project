apiVersion: v1
kind: Pod
metadata:
  name: upload-board-api
  labels:
    app: upload-board-api
    type: back-end

spec:
  containers:
    - name: upload-board-api
      image: bluecode1775/sharenpo:latest
      ports:
        - containerPort: 3000
