apiVersion: v2
name: upload-board-project
description: NestJS REST API backend for the Upload Board Project — authenticated file upload and board management (ADR 0041)
type: application

# Chart version — bump on any template/values change (SemVer).
version: 0.2.0

# Tracks backend/package.json's version, not this chart's own version.
appVersion: "0.0.1"
