## Copy to e2e/.env (git-ignored) and fill in a real seeded superadmin account.
## No in-app flow can create one — run `pnpm e2e:seed` (backend ADR 0013).

E2E_SUPERADMIN_EMAIL=
E2E_SUPERADMIN_PASSWORD=
