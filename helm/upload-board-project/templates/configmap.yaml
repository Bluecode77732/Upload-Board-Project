apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "upload-board-project.fullname" . }}
  labels:
    {{- include "upload-board-project.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | toString | quote }}
  {{- end }}
